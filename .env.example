# Metabase
METABASE_URL=http://localhost:3000
METABASE_USER=admin@etl.local
METABASE_PASSWORD=Admin@123       # change to something stronger — Metabase rejects common passwords

# PostgreSQL (từ ETL repo)
POSTGRES_HOST=etl_postgres
POSTGRES_PORT=5432
POSTGRES_DB=northwind_dw
POSTGRES_USER=dw_user
POSTGRES_PASSWORD=changeme
